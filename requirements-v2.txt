# ==================================================
# Local RAG Chatbot v2 — Dependencies
# Python 3.14 / Windows — Verified June 2025
# ==================================================
#
# Cài đặt:
#   pip install -r requirements-v2.txt

# Core LangChain stack
# NOTE: langchain-community phải <0.4.0 để ragas hoạt động
langchain>=1.3.0
langchain-community>=0.3.0,<0.4.0
langchain-ollama>=1.1.0
langgraph>=1.2.0

# Vector store — FAISS
faiss-cpu>=1.9.0

# Embedding — FastEmbed (local, không cần API)
fastembed>=0.5.0

# Hybrid search & reranker
rank-bm25>=0.2.2
flashrank>=0.2.9

# Document processing
pypdf>=4.0.0
PyMuPDF>=1.24.0

# UI
gradio>=5.0.0

# Evaluation
ragas>=0.4.0
datasets>=2.0.0

# Utils
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
pyyaml>=6.0
pandas>=2.0.0
