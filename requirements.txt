# ==================================================
# Local RAG Chatbot v2 — Agentic RAG Dependencies
# Python 3.10+ / Windows — Verified July 2026
# ==================================================
#
# Cài đặt:
#   pip install -r requirements-v2.txt

# Core LangChain stack
# NOTE: langchain-community phải <0.4.0 để ragas hoạt động
langchain>=1.3.0
langchain-community>=0.3.0,<0.4.0
langchain-ollama>=1.1.0
langchain-text-splitters>=0.3.0
langgraph>=1.2.0

# Vector store — FAISS
faiss-cpu>=1.9.0

# Embedding — FastEmbed (local, không cần API)
fastembed>=0.5.0

# Hybrid search & reranker
rank-bm25>=0.2.2
flashrank>=0.2.9

# Document processing
pypdf>=4.0.0
PyMuPDF>=1.24.0
python-docx>=1.1.0
openpyxl>=3.1.0

# UI
gradio>=5.0.0

# Evaluation
ragas>=0.4.0
datasets>=2.0.0

# Agentic RAG — Web search tool
duckduckgo-search>=7.0.0

# Utils
pydantic-settings>=2.6.0
python-dotenv>=1.0.0
pandas>=2.0.0
